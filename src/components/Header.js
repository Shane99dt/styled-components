import styled from "styled-components";
import { mainDarkBlue } from "./colors/Colors";

const Header = styled.header`
  padding: 0 50px;
  background-color: ${mainDarkBlue};
`;

export default Header;
